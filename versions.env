# Runtime pins for the Windows portable packager.
# DSH_VERSION is normally resolved from upstream dsh-v* tags at release time.
# Fallback tracks the latest upstream tag (may not be on npm yet — build uses git pack).
NODE_VERSION=24.20.0
PNPM_VERSION=11.24.0
DSH_VERSION_FALLBACK=0.1.2-alpha.1
